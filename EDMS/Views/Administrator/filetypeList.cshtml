@model IEnumerable<EDMS.filetype>

@{
    ViewBag.Title = "Filetype List";
    var count = 0;
}

<h2>Filetype List</h2>

<p>
    @Html.ActionLink("Create New", "filetypeCreate")
</p>
<table class="table table-hover table-striped table-bordered" id="myTable">
    <thead>
        <tr>
            <th>
                S/N
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @{ count++; } count
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "filetypeEdit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "filetypeDetails", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "filetypeDelete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th></th>
        </tr>
    </tfoot>
</table>
